# Dershow

موقع Dershow - نسخة الموبايل

## النشر على GitHub Pages
1. ارفع كل الملفات والمجلدات كما هي (index.html + مجلد assets + ملف .nojekyll).
2. من إعدادات المستودع: Settings > Pages > اختر الفرع (main) والمجلد (/root).
3. الموقع يفتح تلقائياً من index.html.

